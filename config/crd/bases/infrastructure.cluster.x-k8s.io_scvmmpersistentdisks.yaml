---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: scvmmpersistentdisks.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: ScvmmPersistentDisk
    listKind: ScvmmPersistentDiskList
    plural: scvmmpersistentdisks
    singular: scvmmpersistentdisk
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: ScvmmMachine that owns disk
      jsonPath: .metadata.ownerReferences[?(@.kind=='ScvmmMachine')].name
      name: OWNER
      type: string
    - description: Storage Path
      jsonPath: .spec.path
      name: PATH
      priority: 1
      type: string
    - description: vhdx Filename
      jsonPath: .spec.filename
      name: FILENAME
      priority: 1
      type: string
    - description: Maximum Disk Size
      jsonPath: .spec.size
      name: SIZE
      type: string
    - description: Current Disk Size
      jsonPath: .status.size
      name: USED
      type: string
    - description: Creation Timestamp
      jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScvmmPersistentDisk is the Schema for the scvmmpersistentdisks
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScvmmPersistentDiskSpec defines the desired state of ScvmmPersistentDisk
            properties:
              dynamic:
                default: true
                description: Specify that the virtual disk can expand dynamically
                type: boolean
              filename:
                description: Filename of stored disk
                type: string
              path:
                description: Location (share path) where disk is stored
                type: string
              size:
                anyOf:
                - type: integer
                - type: string
                description: Size of the virtual disk
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              vmHost:
                description: |-
                  VMHost where vhdx resides,
                  Also indicates if the disk exists or not
                type: string
            required:
            - size
            type: object
          status:
            description: ScvmmPersistentDiskStatus defines the observed state of ScvmmPersistentDisk
            properties:
              size:
                anyOf:
                - type: integer
                - type: string
                description: Current usage (size) of virtual disk
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
